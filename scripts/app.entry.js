/* ERGSN client bundle entry — Plan B-0 placeholder.
 *
 * B-1 will move inline <script> blocks out of index.html (and the other
 * HTML pages) into per-feature modules imported here, so esbuild can
 * produce a single dist/app.js. Until then this is a deliberately empty
 * entry — `npm run build:js` succeeds and emits a near-zero bundle. */
export {};
